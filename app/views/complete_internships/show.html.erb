
<div id="form-block4" class="text-left">
    <h4><%= @complete_internship.student.try(:first_name) %>
      <%= @complete_internship.student.try(:last_name) %>
      <%= t "complete_internships.semester" %>
      <%= @semester_name %>
      <% if @complete_internship.semester_of_study %>
        <%= t "complete_internships.semester_of_study1" %>
        <%= @complete_internship.semester_of_study %>
        <%= t "complete_internships.semester_of_study2" %>
      <% end %>
    </h4>
    <br>
    <div>
      <strong><%= t "complete_internships.aep" %>:</strong>
      <%= t "complete_internships.aep_value.#{@complete_internship.aep}" %>
    </div>
    <div>
      <strong><%= t "complete_internships.passed" %>:</strong>
      <%= t "complete_internships.passed_value.#{@complete_internship.passed}" %>
    </div>
    <br>

    <% if @complete_internship.student %>
      <div class="card-deck">
        <% @complete_internship.internships.each do |internship|%>
          <% accepted = internship.registration_state.nil? %>
          <div class="card border-<%= accepted ? 'secondary' : 'success' %>">
            <div class="card-header"><%= internship.internship_state.try(:name) %></div>
            <div class="card-body">
              <h5 class="card-title"><%= t "complete_internships.internship" %> @
                <% if internship.company_v2 %>
                  <%= internship.company_v2.name %>
                <% else %>
                  <%= t "complete_internships.company" %>
                <% end %></h5>
              <p class="card-text">
                <% if accepted %>
                  <%= render 'checklist' %>
                <% else %>
                  <%= render 'partial_internship', internship: internship %>
                <% end %>
              </p>
            </div>
          </div>
        <% end %>
      </div>
      <strong><%= @complete_internship.student.internships.size == 0 ? "You haven't started an internship application yet" : '' %></strong>
    <% end %>

  <br>
    <%= link_to 'Create New Internship', new_internship_path %>
    <br><br><br>
    <%= link_to 'Edit', edit_complete_internship_path(@complete_internship) %> |
    <%= link_to 'Back', complete_internships_path %>

</div>
