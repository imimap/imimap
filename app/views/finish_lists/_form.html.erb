<%= form_for(@finish_list) do |f| %>
  <% if @finish_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finish_list.errors.count, "error") %> prohibited this finish_list from being saved:</h2>

      <ul>
      <% @finish_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :internship_id %><br>
    <%= f.number_field :internship_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
