<%= form_for(@finish_read) do |f| %>
  <% if @finish_read.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finish_read.errors.count, "error") %> prohibited this finish_read from being saved:</h2>

      <ul>
      <% @finish_read.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :internship_id %><br>
    <%= f.number_field :internship_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
