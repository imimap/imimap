# docker-tool has been deleted.
# instead of                    use
# build                         docker-compose build
# start imimaps (both images)   docker-compose up
# start imimaps, detached       docker-compose up -d
# running the tests             docker-compose run imimaps bundle exec rake spec
# stop                          docker-compose kill
# stop (nicely)                 docker-compose down
# test                          docker exec -ti imimaps-dev bundle exec rake spec
# enter                         docker exec -ti imimaps-dev bundle exec bash

# note that you need to set up the test db before running any tests:
# docker exec -ti imimaps-dev bundle exec rake RAILS_ENV=test db:migrate


# docker ps | grep imimaps
# docker images


# to run the production environment locally:
#
cat $0
